<div class="container">
  <div class="row">
    {{#each flashMessages.queue as |flash|}}
      {{flash-message flash=flash}}
    {{/each}}
  </div>
  <div class="row">
    <section id="immedialist">
      <header>
        <h1 id="title" class="text-center">Immedialist</h1>
      </header>

      {{outlet}}
    </section>
  </div>
</div>
